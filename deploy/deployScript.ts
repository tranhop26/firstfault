export default async function main(_client: unknown): Promise<never> {
  throw new Error(
    "Deployment is disabled for the FirstFault baseline until its Intelligent Contract is implemented.",
  );
}
